package dkeep.gui;

/**
 * This class just holds some boolean to control the MusicThread so it behaves properly
 *
 */
public class Flags {
	protected static boolean flag;	
	protected static boolean activethread;
	protected static boolean stoped;
}
